#include "InstanceTransform.h"
#include "Components/TransformCom.h"

//XVˆ—
void InstanceTransform::Update(float elapsedTime)
{
    //m_cpuinstancedata.get()->position = GetGameObject()->transform_->GetWorldPosition();
    //m_cpuinstancedata.get()->scale = GetGameObject()->transform_->GetScale();
    //m_cpuinstancedata.get()->quaternion = GetGameObject()->transform_->GetRotation();
}